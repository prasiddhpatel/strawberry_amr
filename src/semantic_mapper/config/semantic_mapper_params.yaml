semantic_mapper_node:
  ros__parameters:
    map_frame: map
    dedup_grid_m: 0.10
    # Phase 2's target_manager loads THIS file at startup as the pre-mapped
    # trajectory -- MUST match target_manager's initial_csv_path (same
    # default in both places, set explicitly here rather than relying on
    # the defaults happening to agree).
    csv_path: "~/ros2_ws/maps/semantic_targets.csv"
